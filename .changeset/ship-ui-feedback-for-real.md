---
---

Dashboard and auth UI tidy-up (the code changes intended by #62, which merged with only its changeset): drop the "Signed in as" header line and the workspace name above the tabs, restyle the dashboard nav as an underlined tab bar with room to breathe, and remove the app-description subtitles beneath the logo on every auth screen (sign in, sign up, password reset). No package changes.
