# Changesets

These files record release intent. Run `pnpm changeset` for a publishable change
or `pnpm changeset --empty` for an explicit no-release change. See
[`docs/RELEASING.md`](../docs/RELEASING.md) for the complete workflow.
